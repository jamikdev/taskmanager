# taskmanager
# Jamikdev Task Manager  A terminal CLI app in C++ that manages tasks with data persistence, saving lists to a text file on exit and reloading them on startup.  ## Features * Local storage via standard file streams * Smooth multiline string extraction * Dynamic vector memory management
